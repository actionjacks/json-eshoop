productsEditTemplate;
import { layout } from "../layout";
// import { getError } from "./../../helpers";

export default ({ product }) => {
  return layout({
    content: `
  //  uzupelnic
    `,
  });
};
